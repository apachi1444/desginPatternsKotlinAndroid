class DependencyInjectionPattern {

    // dependency injection aims to separate
    // the concerns of constructing objects and using them, leading to loosely coupled programs.

    //The pattern ensures that an object or function which wants to use a given
    // service should not have to know how to construct those services
}