var letKeyword : String = "let us to make instructions on variables and checks if their not null"

var letExample : String = "val x = number?.let{it = it+1} it returns the last line ( in this case it will be int )"

var applyKeyword : String = "take an object and make some changes on it and returns a new object"

var runKeyword : String = "take an object do some modifications and return nothing"